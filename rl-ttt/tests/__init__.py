"""Test package for rl-ttt.

The `tests` directory is a Python package so pytest can import it cleanly;
the `rl_ttt` package itself is importable from the project root because we
keep it at the top level (no `src/` layout, no installable package).
"""
